"# Implementation Plan - Advanced Logistics Routing & Address Modification\n\nThis plan outlines the changes required to implement two advanced logistics features:\n1. **Branch Routing via Barcode Scan**: Adding the ability to route scanned orders to specific branches/warehouses (setting `branchId`, updating status to `AT_BRANCH`, and clearing `driverId`).\n2. **Active Order Address Modification**: Restoring the ability to edit client addresses for active orders directly from the dashboard and ensuring they are returned to the branch for re-routing.\n\n---\n\n## Proposed Changes\n\n### 1. Backend API Routing\n\n#### [MODIFY] [orders route.ts](file:///C:/Users/hazem/Downloads/matger2/src/app/api/admin/orders/route.ts)\n- Add `branchId` to the destructured body parameters of the `PATCH` handler.\n- If `branchId` is provided (or set to `null`), include it in the database update.\n- Ensure the API handles resetting/updating both `driverId` and `branchId` properly.\n\n#### [MODIFY] [assign-barcode route.ts](file:///C:/Users/hazem/Downloads/matger2/src/app/api/delivery/assign-barcode/route.ts)\n- Add check for scanned branch barcodes/IDs. If a scanned barcode matches a branch, return `{ type: \"BRANCH\", branch: matchedBranch, message: \"...\" }`.\n- Support routing orders to branches when a `branchId` is passed in the request body. If `branchId` is passed, update the order status to `AT_BRANCH`, set `branchId`, and set `driverId: null`.\n\n---\n\n### 2. Frontend Components & Main Dashboard\n\n#### [MODIFY] [OrdersTable.tsx](file:///C:/Users/hazem/Downloads/matger2/src/components/admin/OrdersTable.tsx)\n- Add `branches?: any[]` and `onAssignBranch?: (orderId: string, branchId: string) => Promise<void>` to `OrdersTableProps`.\n- Add state to select the routing mode (`\"DRIVER\"` or `\"BRANCH\"`) and track the active quick-assign target (`activeQuickBranchId`).\n- Update the quick assignment header banner to display a toggle/select between Driver and Branch routing.\n- When in `\"BRANCH\"` mode, show the list of active b
<truncated 1990 bytes>