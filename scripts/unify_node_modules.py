import paramiko

def unify_node_modules():
    client = paramiko.SSHClient()
    client.set_missing_host_key_policy(paramiko.AutoAddPolicy())
    
    host = '82.198.228.182'
    port = 65002
    user = 'u754458241'
    pasw = '@n9qe3KgL'
    
    try:
        client.connect(host, port=port, username=user, password=pasw)
        
        # 1. Ensure nodeapp/node_modules is the source of truth
        # (It already seems to be the target of generate)
        
        target = '/home/u754458241/nodeapp/node_modules'
        
        links = [
            '/home/u754458241/domains/morsall.com/public_html/node_modules',
            '/home/u754458241/domains/morsall.com/public_html/app_new/node_modules',
            '/home/u754458241/domains/morsall.com/nodejs/node_modules'
        ]
        
        for l in links:
            print(f"Linking {l} -> {target}...")
            client.exec_command(f"rm -rf {l} && ln -s {target} {l}")
            
        # 2. Regenerate Prisma
        print("Regenerating Prisma...")
        client.exec_command("export PATH=$PATH:/opt/alt/alt-nodejs22/root/usr/bin && cd /home/u754458241/nodeapp/ && PRISMA_CLIENT_ENGINE_TYPE=library npx prisma generate")
        
        # 3. Restart
        client.exec_command("touch /home/u754458241/domains/morsall.com/nodejs/tmp/restart.txt")
        
        client.close()
        print("Done.")
    except Exception as e:
        print(f"Error: {e}")

if __name__ == "__main__":
    unify_node_modules()
