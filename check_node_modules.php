<?php
$dir = '/home/u754458241/domains/morsall.com/public_html/app_new/node_modules';
if (is_dir($dir)) {
    echo "<pre>Contents of node_modules (partial):\n";
    $items = scandir($dir);
    // Print first 50 items and look for prisma
    $count = 0;
    foreach ($items as $item) {
        if ($item == '.' || $item == '..') continue;
        if (strpos($item, 'prisma') !== false) {
            echo "[PRISMA FOUND] $item\n";
        }
        if ($count < 50) {
            echo $item . "\n";
        }
        $count++;
    }
    echo "Total items: $count\n";
    echo "</pre>";
} else {
    echo "node_modules NOT FOUND.";
}
?>
